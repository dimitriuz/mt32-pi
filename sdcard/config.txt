# mt32-pi boot configuration
boot_delay=0
disable_splash=1
gpu_mem=16
disable_overscan=1

# Uncomment the following line if your HDMI audio extractor isn't outputting any sound.
#hdmi_drive=2

# Uncomment to disable undervoltage/temperature warnings and throttling.
#avoid_warnings=2

[pi02]
arm_64bit=1
kernel=kernel8.img
core_freq=250
core_freq_min=250
# Uncomment the following to overclock the Pi Zero 2 W
#arm_freq=1200
#over_voltage=2

[pi3]
arm_64bit=1
kernel=kernel8.img
core_freq=250
core_freq_min=250

[pi4]
arm_64bit=1
armstub=armstub8-rpi4.bin
kernel=kernel8-rpi4.img

[cm4]
otg_mode=1

[pi5]
# Raspberry Pi 5, 500, 500+ and CM5. Firmware lives in EEPROM, so none of the
# bootcode.bin/start*.elf files are used - this needs only the kernel, a
# bcm2712-*.dtb matching the board and stepping, and overlays/bcm2712d0.dtbo.
#
# Note core_freq is set per-board above rather than globally: it is a Pi 1-4
# concept there (locking the I2C clock), but it is still live on BCM2712, where
# clamping it does harm. gpu_mem above is global but ignored on BCM2712, which
# has no split memory.
kernel=kernel_2712.img
kernel_address=0x80000
